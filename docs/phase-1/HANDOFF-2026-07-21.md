# Session Handoff — 2026-07-21 (Phase 0 close → Phase 1 RLS proven)

**Read this first when resuming.** Then: `docs/phase-0/README.md` (product contract), `docs/phase-1/codex-work-order.md` (Phase 1 plan), `docs/phase-1/01-rls-proof.md` (RLS evidence).

---

## Where the project stands

- **Posture:** preserve-and-harden, NOT rebuild. The app is a substantial full-stack product on the target stack (Next 15 / Clerk / Supabase / Liveblocks / Cloudflare / Anthropic).
- **Source of truth:** local folder `C:\Users\siddi\Documents\The Missing Peace` = canonical; GitHub `master` and `codex/update-prototype-from-zip` both at commit `53357c9`. Emergent scaffold removed.
- **Supabase project:** the app points at `ztgixihhivtharrelmps` (per `.env.local`). NOTE: two *unrelated* projects (`mirepoix`, `shiftcommander` = a restaurant app) live in the same org — do not touch them.
- **Migrations:** `0000`–`0014` have been run on the live DB. `0015` (new, this session) has been pushed too.

## Phase 0 — CLOSED (done earlier, reconciled this session)
- Full audit + product contract in `docs/phase-0/` (8 artifacts + README).
- Codex executed the Phase 0 boundary work; reconciled against disk + git.
- Risks cleared: **R-1 secrets** (`.env.local` ignored/untracked/absent from history), **R-3 one codebase** (Emergent removed, one repo), **R-12 path** (canonical path confirmed; old `…\TMP` doesn't exist).
- Docs moved under `/docs`; suite reported at **42/42** tests.

## Phase 1 — headline delivered this session: RLS PROVEN (R-2)
Proved workspace isolation as **verified evidence**, not a claim:
- Loaded the real migrations into a throwaway Postgres 16 mirror; ran an isolation proof as role `authenticated`.
- **9/9 core checks pass:** member A cannot read or write member B's `board_items`/`budget_items`/`decisions`/`guests` (writes rejected, SQLSTATE 42501); B cannot see A's private Peace Note.
- **Gap found + fixed:** `0001` left `workspaces` and `users` without RLS (latent enumeration risk). Added **`0015_rls_harden_identity_tables.sql`** (security-definer helper avoids policy recursion). Re-ran proof → **11/11 pass, Peace Notes still intact**.

### Files delivered this session (staged on disk — commit on your machine, NOT from the sandbox)
- `supabase/migrations/0015_rls_harden_identity_tables.sql` — hardening fix (applied to live DB already).
- `supabase/tests/rls_proof.sql` — portable proof (SQL editor or `psql`); transaction-wrapped; **non-prod only** (it truncates seeded identities).
- `tests/integration/rls-isolation.test.ts` — end-to-end Clerk-JWT test (skipped unless `RLS_LIVE=1`; refuses to run against the prod project ref).
- `docs/phase-1/01-rls-proof.md` — full evidence, the gap, and run steps.

## Owner setup status
- ✅ `0015` pushed.
- ✅ Clerk → Supabase integration enabled.
- ❓ **Confirm the second switch:** Supabase → Authentication → Third-Party Auth → add **Clerk**. Without it, Supabase won't trust Clerk tokens and `supabaseForUser()` requests fail.
- `rls_proof.sql` does NOT need to be run on production — it's a test, already run against a mirror (11/11).

## Do next (in the new session)
1. **Live corroboration (safe, read-only):** once the `ztgixihhivtharrelmps` Supabase MCP connects, confirm `0001`–`0015` applied and run **Security Advisor** to independently flag any remaining RLS gaps.
2. **Reconcile owner WIP (T0):** uncommitted on disk — `app/(app)/canvas/`, `src/lib/canvas/`, `supabase/migrations/0014_canvas_state.sql`, plus modified `src/components/nav.tsx` and prototype/landing files. Decide commit vs. quarantine; validate 0014.
3. **Remaining Phase 1:** design-system primitives (T1) · app-shell hardening — mobile drawer, save/sync states, error boundaries (T3) · observability + analytics event-taxonomy stub (T4).
4. Optional: run the end-to-end integration test on a **staging** project with two Clerk test users.

## Guardrails (carry forward)
- **Never commit from the Cowork sandbox** — mount staleness can snapshot corrupted files (R-5). Commit on the owner's machine.
- Detect truncation with an esbuild parse sweep, not byte length.
- Migrations are append-only + ordered; keep `0000_reset` in sync; pglast-validate before applying.
- Weaver/AI stays deterministic-guarded, cited, bounded (Phase 2 work).
