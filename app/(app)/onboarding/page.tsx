import { DreamWalkOnboarding } from '@/app/(app)/onboarding/DreamWalkOnboarding';

export default function Onboarding() {
  return (
    <div className="relative">
      <DreamWalkOnboarding />
    </div>
  );
}
