export default function Page() {
  return (
    <div className="max-w-3xl">
      <h1 className="voice text-3xl">Peace Notes</h1>
      <p className="text-sm text-[var(--ink-soft)] mt-2">The private emotional layer — letters, vows, gratitude, dedications, memories. Locked + visibility rules. Build Plan v2 §6½, Phase 3.</p>
      <p className="text-xs text-[var(--ink-faint)] mt-6">Stub — see HANDOFF.md.</p>
    </div>
  );
}
