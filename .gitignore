node_modules/
.pnpm-store/
.next/
out/
# Anchored to the repository root on 2026-07-26. Unanchored, `build/` matched
# `src/lib/build/` and silently excluded the build-stamp module from the commit
# while both routes that import it were committed. See One-Engine 26.4.
/build/
.env
.env.local
.env*.local
*.log
*.zip
.DS_Store
.vercel
.open-next/
.pages-prototype/
cloudflare-prototype/dist/
.wrangler/
cloudflare-env.d.ts
*.tsbuildinfo
next-env.d.ts
coverage/
tmp/
# Staging folder for files awaiting manual deletion: this mount forbids unlink,
# so files to be removed are moved here instead. Anchored to the root for the
# same reason `/build/` is. Never track it.
/_to_delete/

# Credentials for the agentic test accounts. Never tracked.
docs/access/
